Create a new Next.js + Tailwind website called "Safarly - Travel Companion", a premium AI multi-agent tourism companion for Saudi Arabia. This first task is ONLY the foundation: design system, fonts, theme, navigation shell, landing page, and placeholder routes. No feature logic yet.
 
I uploaded DESIGN.md (the "Adventurous Precision" design system) to the project root - treat it as the single source of truth and follow it exactly: strict 4px spacing grid (1280px max width, 24px gutters, 16px mobile margins), tonal layering + 1px borders for depth (only one diffuse shadow for floating elements), soft radii (4px standard elements, 8px cards/modals, pill only for progress/status), and its component specs - Primary / Secondary / "Adventure Action" (neon mint bg + night-sky text) buttons, bordered inputs that focus to accent, technical chips with --primary-soft background, and the itinerary timeline as a 2px border-color line with accent nodes. The tokens below wire DESIGN.md into globals.css.
 
DESIGN SYSTEM - create these exact tokens as CSS variables in globals.css and wire them into Tailwind:
Light mode ("Light Grid"): --bg #F5F7FA, --surface #FFFFFF, --surface-alt #ECEEF3, --border #E2E6ED, --text #141920 (deep charcoal), --text-muted #5C6472, --primary #121826 (night sky - nav, headers, primary buttons), --primary-hover #232B3D, --primary-soft #E7E9EE, --accent #00D8A4 (neon mint - CTAs and highlights ONLY), --accent-hover #00BD8F, --indigo #5C6CFF (neon indigo - secondary highlights, links, badges), --indigo-hover #4453E6, --success #16C98A, --warning #FFB020, --error #FF4D6D.
Dark mode ("Night Sky", the DEFAULT theme): --bg #0A0E16, --surface #121826 (card/panel backgrounds), --surface-alt #1A2233, --border #232C3D, --text #EDEFF3, --text-muted #8A93A6, --primary #5C6CFF (indigo takes over as primary once night sky becomes the background itself), --primary-hover #7683FF, --primary-soft #141C2C, --accent #00D8A4 (same neon mint, unchanged - CTAs and highlights ONLY), --accent-hover #33E6BC, --indigo #5C6CFF, --indigo-hover #7683FF, --success #33E0A0, --warning #FFC24D, --error #FF6B85.
 
BRAND IDENTITY: name "Safarly" (from the Arabic "safar" - travel), tagline "Travel Companion". The approved logo is a circular line-art mark - a rising sun over a winding desert road - in a neon gradient from mint #00D8A4 to indigo #5C6CFF with a soft glow on dark backgrounds. I will upload it as /public/safarly-logo.png; reserve its spot in the navbar (28px, next to the wordmark "Safarly" in the display font semibold) and large on the landing hero. On dark surfaces the wordmark may use the mint-to-indigo gradient as text fill; on light surfaces it is solid --primary. The gradient is reserved for the logo and wordmark ONLY - never on buttons or body text.
 
TYPOGRAPHY (per DESIGN.md): "Inter" EXCLUSIVELY for all Latin text - headlines in Bold/ExtraBold with tight letter-spacing (-0.02em), display 48/56 (36/42 on mobile), headline-md 24/32, body 16/24, labels 12-14px semibold with slight tracking, all-caps only for small category tags. "IBM Plex Sans Arabic" for Arabic text. No other fonts.
 
LOOK & FEEL: premium neon-tech. Dark mode: deep night-sky surfaces, a subtle dotted-grid background texture, sparing neon-mint glow on primary CTAs, indigo for secondary highlights - Linear/Vercel-level polish. Light mode ("Light Grid"): near-white background, night-sky navy for nav, headers and primary buttons, mint reserved for the hero CTA, indigo outline buttons and links. Generous whitespace on the 4px grid, 8px-radius cards with 1px borders, depth via tonal layering not shadows (per DESIGN.md). Never neon-overload: mint appears at most twice per screen.
 
THEMES: both modes are first-class citizens. Dark "Night Sky" is the default; a sun/moon toggle in the navbar switches instantly with a smooth transition, the choice persists in localStorage, and every component must be designed against BOTH token sets from day one - never hardcode a color.
 
LANGUAGE & RTL: the site is fully bilingual English + Arabic from the start. Build a lightweight i18n layer with all user-facing strings in /locales/en.json and /locales/ar.json (no hardcoded strings anywhere), and a language switcher in the navbar ("EN | العربية"). Selecting Arabic sets lang="ar" and dir="rtl" on <html>, switches the UI font to IBM Plex Sans Arabic, and MIRRORS the layout properly: navbar order, chevrons/arrows, card alignment, timeline side, progress direction, margins/paddings (use Tailwind logical properties: ms-/me-/ps-/pe-/start/end - never ml-/mr- directly). Language choice persists in localStorage. Provide natural, native-quality Arabic translations - not machine-literal.
 
MOBILE: build mobile-FIRST - most travelers will use this on a phone. At mobile widths (<768px): a fixed bottom navigation bar with 4 icons (Home, Plan, Lens, Dialect) styled like a native app, safe-area-inset padding for notched phones, all touch targets at least 44x44px, primary CTAs thumb-reachable near the bottom, no horizontal scroll ever, momentum scrolling, and instant :active tap states on every tappable element. The desktop top navbar collapses to logo + theme/language toggles on mobile. Perfect at 360, 390, 430, 768, 1024, 1440 px.
 
BUILD: sticky top navbar (logo spot + wordmark + links: Plan a Trip, Live Lens, Dialect, Dashboard + theme toggle + language switcher), elegant footer, and stub pages at /onboarding /trip /generating /itinerary /lens /dialect /dashboard, each one styled "coming soon" card localized in both languages.
 
LANDING PAGE at / : full hero - a small Arabic greeting in --accent above the headline, headline "Your Saudi Journey, Intelligently Crafted" in Space Grotesk, one-sentence subtext, primary CTA "Start Planning" (mint, linking to /onboarding), ghost CTA "See How It Works" (indigo outline); a 5-card feature row (Trip Planner, Dialect Tutor, Live Lens, Live Translation, Local Guides) each with a line icon; an "agents at work" strip with 8 small agent chips (Goal Analyzer, Destination Discovery, Restaurant, Budget, Culture, Safety, Sustainability, Verification); a Vision 2030 alignment band; final CTA section. CSS-only dotted-grid dividers between sections - no stock photos. Mobile-perfect in one pass.
