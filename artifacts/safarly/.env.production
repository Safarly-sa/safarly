# Loaded automatically by `vite build` (mode=production). Not secret — just
# the public URL of the deployed API server, so it's safe to commit. See
# .env.example for what VITE_API_URL does and why it's not needed for local dev.
VITE_API_URL=https://safarly-api.onrender.com
